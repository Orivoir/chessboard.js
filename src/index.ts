/**
 * For the therms `client` inside all code files take reference to developers that use
 * this library and the therms `user` is a reference to final user that consume a application
 * that import this library (in other therms `user` is probably a player).
 */